zabbix_export:
  version: '7.4'
  media_types:
    - name: 'VK Teams'
      type: WEBHOOK
      parameters:
        - name: HTTPProxy
        - name: Message
          value: '{ALERT.MESSAGE}'
        - name: Subject
          value: '{ALERT.SUBJECT}'
        - name: To
          value: '{ALERT.SENDTO}'
        - name: Token
      script: |
        var VKTeams = {
            token: null,
            to: null,
            message: null,
            proxy: null,
        
            sendMessage: function () {
                var params = {
                    token: VKTeams.token,
                    chatId: VKTeams.to,
                    text: VKTeams.message
                },
                data,
                response,
                request = new HttpRequest(),
                url = 'https://myteam.mail.ru/bot/v1/messages/sendText';
        
                if (VKTeams.proxy) {
                    request.setProxy(VKTeams.proxy);
                }
        
                data = Object.keys(params).map(function(key) {
                    return encodeURIComponent(key) + '=' + encodeURIComponent(params[key]);
                }).join('&');
        
                // Remove replace() function if you want to see the exposed token in the log file.
                Zabbix.log(4, '[VKTeams Webhook] URL: ' + url);
                Zabbix.log(4, '[VKTeams Webhook] params: ' + data.replace(VKTeams.token, '<TOKEN>'));
                response = request.get(url + '?' + data);
                Zabbix.log(4, '[VKTeams Webhook] HTTP code: ' + request.getStatus());
        
                try {
                    response = JSON.parse(response);
                }
                catch (error) {
                    response = null;
                }
        
                if (request.getStatus() !== 200 || typeof response.ok !== 'boolean' || response.ok !== true) {
                    if (typeof response.description === 'string') {
                        throw response.description;
                    }
                    else {
                        throw 'Unknown error. Check debug log for more information.';
                    }
                }
            }
        };
        
        try {
            var params = JSON.parse(value);
        
            if (typeof params.Token === 'undefined') {
                throw 'Incorrect value is given for parameter "Token": parameter is missing';
            }
        
            VKTeams.token = params.Token;
        
            if (params.HTTPProxy) {
                VKTeams.proxy = params.HTTPProxy;
            } 
        
            VKTeams.to = params.To;
            VKTeams.message = params.Subject + '\n' + params.Message;
        
            VKTeams.sendMessage();
        
            return 'OK';
        }
        catch (error) {
            Zabbix.log(4, '[VKTeams Webhook] notification failed: ' + error);
            throw 'Sending failed: ' + error + '.';
        }
      process_tags: 'YES'
      description: |
        Template for VK Teams created from telegram template
        https://git.zabbix.com/projects/ZBX/repos/zabbix/browse/templates/media/telegram
        
        1. Register bot: send "/newbot" to @metabot and follow instructions
        2. Copy and paste the obtained token into the "Token" field above
      message_templates:
        - event_source: TRIGGERS
          operation_mode: PROBLEM
          subject: 'Problem: {EVENT.NAME}'
          message: |
            Problem started at {EVENT.TIME} on {EVENT.DATE}
            Problem name: {EVENT.NAME}
            Host: {HOST.NAME}
            Severity: {EVENT.SEVERITY}
            Operational data: {EVENT.OPDATA}
            Original problem ID: {EVENT.ID}
            {TRIGGER.URL}
        - event_source: TRIGGERS
          operation_mode: RECOVERY
          subject: 'Resolved in {EVENT.DURATION}: {EVENT.RECOVERY.NAME}'
          message: |
            Problem has been resolved in {EVENT.DURATION} at {EVENT.RECOVERY.TIME} on {EVENT.RECOVERY.DATE}
            Problem name: {EVENT.RECOVERY.NAME}
            Host: {HOST.NAME}
            Severity: {EVENT.SEVERITY}
            Original problem ID: {EVENT.ID}
            {TRIGGER.URL}
        - event_source: TRIGGERS
          operation_mode: UPDATE
          subject: 'Updated problem: {EVENT.NAME}'
          message: |
            {USER.FULLNAME} {EVENT.UPDATE.ACTION} problem at {EVENT.UPDATE.DATE} {EVENT.UPDATE.TIME}.
            {EVENT.UPDATE.MESSAGE}
            
            Current problem status is {EVENT.STATUS}, acknowledged: {EVENT.ACK.STATUS}.
        - event_source: DISCOVERY
          operation_mode: PROBLEM
          subject: 'Discovery: {DISCOVERY.DEVICE.STATUS} {DISCOVERY.DEVICE.IPADDRESS}'
          message: |
            Discovery rule: {DISCOVERY.RULE.NAME}
            
            Device IP: {DISCOVERY.DEVICE.IPADDRESS}
            Device DNS: {DISCOVERY.DEVICE.DNS}
            Device status: {DISCOVERY.DEVICE.STATUS}
            Device uptime: {DISCOVERY.DEVICE.UPTIME}
            
            Device service name: {DISCOVERY.SERVICE.NAME}
            Device service port: {DISCOVERY.SERVICE.PORT}
            Device service status: {DISCOVERY.SERVICE.STATUS}
            Device service uptime: {DISCOVERY.SERVICE.UPTIME}
        - event_source: AUTOREGISTRATION
          operation_mode: PROBLEM
          subject: 'Autoregistration: {HOST.HOST}'
          message: |
            Host name: {HOST.HOST}
            Host IP: {HOST.IP}
            Agent port: {HOST.PORT}
